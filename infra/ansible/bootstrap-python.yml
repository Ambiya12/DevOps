---
- name: Bootstrap Python on fresh Ubuntu containers
  hosts: all
  gather_facts: false

  tasks:
    - name: Install python3, python3-apt and sudo if missing
      raw: test -x /usr/bin/python3 || (apt-get update && apt-get install -y python3 python3-apt sudo)
      changed_when: false
